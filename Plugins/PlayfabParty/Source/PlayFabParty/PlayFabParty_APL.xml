<?xml version="1.0" encoding="utf-8"?>
<root xmlns:android="http://schemas.android.com/apk/res/android">
    <init>
        <log text="PlayFabParty Plugin Init"/>
    </init>

    <resourceCopies>
        <log text="Copying libparty.so"/>
        <!-- note: have to stage this since we linked it -->
        <copyFile src="$S(AbsPluginDir)/../../ThirdParty/Android/libparty.so"
                  dst="$S(BuildDir)/libs/$S(Architecture)/libparty.so" />
    </resourceCopies>
</root>
